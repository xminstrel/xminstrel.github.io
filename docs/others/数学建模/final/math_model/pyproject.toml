[project]
name = "zhejiang-league-model"
version = "0.1.0"
description = "Self-contained modeling pipeline for the Zhejiang city/county football league grouping problem."
requires-python = ">=3.10"

[tool.pytest.ini_options]
testpaths = ["tests"]
addopts = "-q"
