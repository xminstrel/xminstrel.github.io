# 数模3最终方案摘要

本次已在 `数模3` 中重新跑通数据、图染色、分组、抽签、赛地和赛制仿真。

## 数据

融合数据文件为 `数模3/data/final_teams.csv`。队伍名单来自赛题结构化表，GDP 和人口优先使用 `数模1` 的浙江统计年鉴解析数据。64 支队伍中，52 支匹配到年鉴 GDP 和人口，11 支市级队及景宁畲族自治县使用代理值。

## 图染色

冲突图共有 64 个顶点、232 条边。11 支市级队构成 \(K_{11}\)，所以色数至少为 11；程序构造出 11 色着色，因此强化行政回避下 \(\chi(G)=11\)。最终采用 16 组，是为了匹配 64 队均分、8 个赛地每地 2 组和 32 强淘汰赛。

## 分组

最终分组按“硬约束全部满足后，以 AHP-熵权-TOPSIS 综合评价最高”为准，而不是只看直接搜索目标值。新推荐分组为 D003，TOPSIS 综合评价 0.676844，直接目标值 0.754332，实力方差 11.06，实力极差 9.55，平均组内距离 187.19 km，行政硬约束和同市重复均为 0。

## 抽签

重新生成 3000 个候选方案，取前 100 个形成优质方案池。方案池 SHA-256 为：

```text
c8edf836d1c77bfe7d4ef1765819dc748463ad8f90aec415526d9586fc70b799
```

现场数字串 `7392048615` 对应抽中编号 `q=39`，抽中方案 P0039，TOPSIS 得分 0.568026。

## 赛地

基于最终 D003 分组，推荐承办地为：

台州市、宁波市、杭州市、温州市、绍兴市、金华市、青田县、龙游县。

平均到赛地距离 135.16 km，单队最远距离 287.09 km，覆盖 6 个区域，县级承办点 2 个。

## 赛制

推荐 16 组单循环、每组前二晋级、32 强单败淘汰，总场次 127 场。双循环公平性更高，但总场次增至 223 场，增幅约 75.6%，不作为主方案。
